using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class dialogueParse : MonoBehaviour
{
   /* public Text mainText;
    private Sprite mainSprite;
    public Sprite[] sprites;
    public Sprite backGround;
    public int spriteIndex = 0;
    public int scriptIndex = 0;
    public int picIndex = 0;
    // Use this for initialization
    void Start()
    {
        //mainText = transform.GetChild(0).GetComponent<UnityEngine.UI.Text>();
        //mainSprite = transform.GetChild(2).GetComponent<SpriteRenderer>().sprite;
        //sprites = Resources.LoadAll<Sprite>(spriteNames);
    }

    // Update is called once per frame
    void Update()
    {
        mainText.color = Color.black;
       // mainText.text = Script.scripts[scriptIndex];
        if (Input.GetMouseButtonDown(0)) //left click
        {
            scriptIndex++;

            //update picture spriteR.sprite = sprites[spriteIndex];
        }
    }*/
}
